
// AsyncStorage keys for Twilio credentials
export const STORAGE_KEYS = {
 twilio: ["accountSID", "AuthToken", "TwilioPhoneNumber"],
};

export const API_ENDPOINTS = {
  // LOGIN: '/auth/login',
  // PROFILE: '/user/profile',
  // POSTS: '/posts',
};

export const APP_CONFIG = {
  // VERSION: '1.0.0',
  // TIMEOUT: 10000,
  // RETRY_ATTEMPTS: 3,
};